"""
This is the test suite for the ACE project.

The tests use the `unittest` module to test the functionality of the ACE
digital assistant. The tests are divided into several modules, each of which
tests a different aspect of the program.

To run the tests, execute the following command from the root directory of the
project:

    `python -m unittest discover tests`

This will run all the tests in the `tests` directory and its subdirectories.
"""
